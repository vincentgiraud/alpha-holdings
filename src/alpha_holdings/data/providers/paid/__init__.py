"""Paid data provider adapters (placeholder — not yet implemented).

Future paid adapters (Bloomberg, FactSet, LSEG) live here. Each must
implement the relevant ABCs from ``alpha_holdings.data.providers.base`` and
pass the contract tests in ``tests/test_provider_contracts.py``.
"""
